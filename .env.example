# ==============================================================================
# DEPLOYMENT CONFIGURATION
# ==============================================================================
# Este archivo contiene TODAS las variables de entorno necesarias para ejecutar
# el stack completo (Backend Django + Frontend Next.js + MariaDB)
# 
# INSTRUCCIONES:
# 1. Copia este archivo a .env:  cp .env.example .env
# 2. Rellena los valores según tu entorno (desarrollo/producción)
# 3. NUNCA subas el archivo .env al repositorio
# ==============================================================================

# ==============================================================================
# DATABASE - MariaDB Configuration
# ==============================================================================
DATABASE_NAME=db_arte
DATABASE_USER=artex_user
DATABASE_PASSWORD=CAMBIA_ESTE_PASSWORD_SEGURO
DATABASE_HOST=db
DATABASE_PORT=3306
MARIADB_ROOT_PASSWORD=CAMBIA_ESTE_ROOT_PASSWORD_SEGURO

# ==============================================================================
# DJANGO BACKEND Configuration
# ==============================================================================
# Django Secret Key - Genera uno nuevo con: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=django-insecure-GENERA_UNA_NUEVA_SECRET_KEY_AQUI

# Debug mode (False en producción)
DEBUG=False

# Frontend URL para CORS/CSRF (la URL pública de tu frontend)
FRONTEND_URL=http://localhost:3000

# S3 Media Storage (opcional - dejar vacío para usar almacenamiento local)
USE_S3_MEDIA=true

# ==============================================================================
# AWS S3 Configuration (Backend y Frontend)
# ==============================================================================
AWS_S3_CUSTOM_DOMAIN=d2iqkgcoua86dq.cloudfront.net
AWS_ACCESS_KEY_ID=TU_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=TU_AWS_SECRET_ACCESS_KEY
AWS_STORAGE_BUCKET_NAME=tu-bucket-name

# URLs públicas para el frontend
NEXT_PUBLIC_AWS_S3_CUSTOM_DOMAIN=d2iqkgcoua86dq.cloudfront.net
NEXT_PUBLIC_AX_STORAGE_URL=https://artex.la

# ==============================================================================
# EMAIL Configuration (Backend)
# ==============================================================================
# Gmail SMTP
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-app-password-aqui

# SendGrid (opcional)
SENDGRID_API_KEY=SG.TU_SENDGRID_API_KEY_AQUI
DEFAULT_FROM_EMAIL=Artex <tu-email@ejemplo.com>

# ==============================================================================
# NEXTJS FRONTEND Configuration
# ==============================================================================
# Backend URL para el frontend (en Docker usa /bapi con proxy interno)
NEXT_PUBLIC_BACKEND_URL=/bapi

# NextAuth Configuration - Genera uno con: openssl rand -base64 32
NEXTAUTH_SECRET=GENERA_UN_SECRET_ALEATORIO_DE_32_CARACTERES
NEXTAUTH_URL=http://localhost:3000

# ==============================================================================
# NOTAS IMPORTANTES
# ==============================================================================
# 
# DESARROLLO LOCAL:
# - FRONTEND_URL=http://localhost:3000
# - NEXTAUTH_URL=http://localhost:3000
# - DATABASE_HOST=db (usa el servicio Docker)
# - DEBUG=True (opcional para desarrollo)
#
# PRODUCCIÓN:
# - FRONTEND_URL=https://tu-dominio.com
# - NEXTAUTH_URL=https://tu-dominio.com
# - DEBUG=False
# - Usa passwords seguros y únicos
# - Considera usar Docker secrets o variables de entorno del sistema
#
# SEGURIDAD:
# - NUNCA subas este archivo con valores reales al repositorio
# - Rota las credenciales periódicamente
# - Usa diferentes credenciales para dev/staging/prod
# ==============================================================================
