# 🏗️ Arquitectura de Deployment

## 📐 Diagrama de la Arquitectura

```
┌─────────────────────────────────────────────────────────────────┐
│                     GITHUB REPOSITORIES                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │ dj_py_bck (Backend)│  │ nx_js_ft (Frontend)│                │
│  │  - Django API      │  │  - Next.js App     │                │
│  │  - Dockerfile      │  │  - Dockerfile.remote│               │
│  └────────────────────┘  └────────────────────┘                │
│           ▲                       ▲                              │
│           │                       │                              │
│           │  git clone            │  git clone                   │
│           │  (automático)         │  (automático)                │
└───────────┼───────────────────────┼──────────────────────────────┘
            │                       │
            │                       │
┌───────────┼───────────────────────┼──────────────────────────────┐
│           │   DOCKER COMPOSE      │                              │
│           │   (docker-compose.    │                              │
│           │    remote.yml)        │                              │
├───────────┴───────────────────────┴──────────────────────────────┤
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                    .env (Único archivo)                  │    │
│  │  - DATABASE_*     - AWS_*         - EMAIL_*             │    │
│  │  - DJANGO_*       - NEXT_PUBLIC_* - NEXTAUTH_*          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                              │                                   │
│                              │ Inyecta variables                 │
│                              ▼                                   │
│  ┌────────────┬──────────────────────┬─────────────────────┐    │
│  │            │                      │                     │    │
│  │ ┌──────────▼─────┐  ┌────────────▼──────┐  ┌─────────▼──┐  │
│  │ │   MariaDB      │  │    Backend        │  │  Frontend  │  │
│  │ │   (db)         │  │    (Django)       │  │  (Next.js) │  │
│  │ │                │  │                   │  │            │  │
│  │ │ Port: 3307     │  │ Port: 8000        │  │ Port: 3000 │  │
│  │ │                │  │                   │  │            │  │
│  │ │ - db_arte      │  │ - API REST        │  │ - SSR      │  │
│  │ │ - Volumes:     │  │ - Admin Panel     │  │ - API Proxy│  │
│  │ │   dbdata       │  │ - Migrations auto │  │   /bapi    │  │
│  │ │                │  │ - Static files    │  │ - Auth     │  │
│  │ └────────────────┘  │ - Media files     │  └────────────┘  │
│  │         ▲           │ - S3 Integration  │        │         │
│  │         │           └───────────────────┘        │         │
│  │         │                    ▲                   │         │
│  │         │ MySQL              │ HTTP              │         │
│  │         │ 3306               │ 8000              │         │
│  │         └────────────────────┘                   │         │
│  │                                                   │         │
│  └───────────────────────────────────────────────────┼─────────┘
│                                                      │           │
└──────────────────────────────────────────────────────┼───────────┘
                                                       │
                                            HTTP :3000 │
                                                       │
                                                       ▼
                                              ┌─────────────────┐
                                              │     USUARIO     │
                                              │   (Browser)     │
                                              └─────────────────┘
```

## 🔄 Flujo de Comunicación

```
┌──────────────┐
│   Usuario    │
│  (Browser)   │
└──────┬───────┘
       │
       │ HTTP :3000
       ▼
┌──────────────────────┐
│   Next.js Frontend   │
│   (Container)        │
│                      │
│  Rutas:              │
│  /        → Home     │
│  /login   → Auth     │
│  /catalog → Catalog  │
│  /bapi/*  → Proxy    │──┐
└──────────────────────┘  │
                          │ Rewrite interno
                          │ /bapi → http://backend:8000
                          │
                          ▼
                   ┌──────────────────────┐
                   │   Django Backend     │
                   │   (Container)        │
                   │                      │
                   │  Endpoints:          │
                   │  /api/auth           │
                   │  /api/catalog        │
                   │  /api/stands         │
                   │  /admin              │
                   └──────┬───────────────┘
                          │
                          │ MySQL/MariaDB
                          │ Port 3306
                          ▼
                   ┌──────────────────────┐
                   │   MariaDB Database   │
                   │   (Container)        │
                   │                      │
                   │  - db_arte           │
                   │  - Persistent volume │
                   └──────────────────────┘
```

## 🗂️ Estructura de Variables de Entorno

```
.env (Único archivo en Grupo-4-Construccion/)
│
├── DATABASE_* ──────────────► MariaDB Container
│   ├── DATABASE_NAME         (Nombre DB)
│   ├── DATABASE_USER         (Usuario)
│   ├── DATABASE_PASSWORD     (Password)
│   └── DATABASE_HOST=db      (Hostname en Docker network)
│
├── DJANGO_* ────────────────► Backend Container
│   ├── DJANGO_SECRET_KEY     (Django secret)
│   ├── DEBUG                 (Debug mode)
│   └── FRONTEND_URL          (CORS/CSRF)
│
├── AWS_* ───────────────────► Backend + Frontend
│   ├── AWS_ACCESS_KEY_ID     (S3 credentials)
│   ├── AWS_SECRET_ACCESS_KEY (S3 credentials)
│   ├── AWS_STORAGE_BUCKET    (Bucket name)
│   └── AWS_S3_CUSTOM_DOMAIN  (CloudFront)
│
├── EMAIL_* ─────────────────► Backend Container
│   ├── EMAIL_HOST_USER       (SMTP user)
│   ├── EMAIL_HOST_PASSWORD   (SMTP password)
│   └── SENDGRID_API_KEY      (SendGrid)
│
└── NEXT_* ──────────────────► Frontend Container
    ├── NEXT_PUBLIC_BACKEND_URL=/bapi
    ├── NEXTAUTH_SECRET       (Auth secret)
    ├── NEXTAUTH_URL          (Public URL)
    └── NEXT_PUBLIC_AWS_*     (S3 public vars)
```

## 🚦 Flujo de Deployment

```
1. CLONE
   ┌─────────────────────────────────┐
   │ git clone Grupo-4-Construccion  │
   └────────────┬────────────────────┘
                │
2. CONFIGURE   │
   ┌────────────▼────────────────────┐
   │ cp .env.example .env            │
   │ Editar .env con valores reales  │
   └────────────┬────────────────────┘
                │
3. BUILD       │
   ┌────────────▼────────────────────┐
   │ docker compose up -d            │
   │                                 │
   │ Docker hace:                    │
   │ ├─ Clone dj_py_bck desde GitHub│
   │ ├─ Clone nx_js_ft desde GitHub │
   │ ├─ Build backend Dockerfile    │
   │ ├─ Build frontend Dockerfile   │
   │ ├─ Create Docker network        │
   │ └─ Start all containers         │
   └────────────┬────────────────────┘
                │
4. INITIALIZE  │
   ┌────────────▼────────────────────┐
   │ MariaDB:                        │
   │ ├─ Create database              │
   │ └─ Load initdb/dump.sql         │
   │                                 │
   │ Backend:                        │
   │ ├─ Wait for DB ready            │
   │ ├─ Run migrations               │
   │ ├─ Collect static files         │
   │ └─ Start Django server          │
   │                                 │
   │ Frontend:                       │
   │ ├─ Wait for backend healthy     │
   │ └─ Start Next.js server         │
   └────────────┬────────────────────┘
                │
5. READY       │
   ┌────────────▼────────────────────┐
   │ ✅ Backend:  :8000              │
   │ ✅ Frontend: :3000              │
   │ ✅ Database: :3307              │
   └─────────────────────────────────┘
```

## 🔒 Seguridad

```
┌──────────────────────────────────────────┐
│         Archivo .env                     │
│  (Contiene TODOS los secretos)          │
│                                          │
│  ❌ NUNCA se sube a GitHub               │
│  ✅ Está en .gitignore                   │
│  ✅ Solo existe localmente               │
└──────────────────────────────────────────┘
          │
          │ Se usa como template
          ▼
┌──────────────────────────────────────────┐
│       Archivo .env.example               │
│  (Template SIN valores reales)           │
│                                          │
│  ✅ SÍ se sube a GitHub                  │
│  ✅ Contiene placeholders                │
│  ✅ Documenta variables necesarias       │
└──────────────────────────────────────────┘
```

## 📦 Volúmenes Persistentes

```
Docker Volumes (Persisten datos entre reinicios):

┌─────────────────────────────────────────┐
│  dbdata:                                │
│  ├─ /var/lib/mysql (MariaDB data)      │
│  └─ Persiste toda la base de datos     │
├─────────────────────────────────────────┤
│  staticdata:                            │
│  ├─ /app/staticfiles (Django static)   │
│  └─ CSS, JS, admin static files        │
├─────────────────────────────────────────┤
│  mediadir:                              │
│  ├─ /app/media (User uploads)          │
│  └─ Imágenes subidas por usuarios      │
│     (si no se usa S3)                   │
└─────────────────────────────────────────┘
```

## 🌐 Puertos Expuestos

```
Host Machine          →    Docker Container
─────────────────────────────────────────────
localhost:3000        →    frontend:3000
localhost:8000        →    backend:8000
localhost:3307        →    db:3306
```

## 🔄 Actualización de Código

```
┌──────────────────────────────────────────┐
│  Desarrollador hace push a GitHub       │
│  ├─ dj_py_bck (backend)                 │
│  └─ nx_js_ft (frontend)                 │
└────────────┬─────────────────────────────┘
             │
             │ Push a GitHub
             ▼
┌──────────────────────────────────────────┐
│  GitHub Repositories actualizados        │
└────────────┬─────────────────────────────┘
             │
             │ Rebuild en servidor
             ▼
┌──────────────────────────────────────────┐
│  En servidor de deployment:              │
│  $ docker compose build --no-cache       │
│  $ docker compose up -d                  │
│                                          │
│  Docker:                                 │
│  ├─ Clona última versión desde GitHub   │
│  ├─ Reconstruye imágenes                │
│  ├─ Reinicia containers                 │
│  └─ Aplica cambios                      │
└──────────────────────────────────────────┘
```

---

**Esta arquitectura permite:**
- ✅ Deployment con un solo comando
- ✅ Actualización fácil desde GitHub
- ✅ Configuración centralizada en un `.env`
- ✅ Separación de concerns (3 repos independientes)
- ✅ Escalabilidad y mantenibilidad
